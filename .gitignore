/target
.DS_Store

# Personal dev tooling — OpenWolf context + Claude Code config. Machine-local, not
# shared: the hooks (.claude/settings.json → .wolf/hooks/*.js) and the instructions
# (CLAUDE.md, .claude/rules/) keep working from disk; they just aren't committed.
.wolf/
.claude/
CLAUDE.md
